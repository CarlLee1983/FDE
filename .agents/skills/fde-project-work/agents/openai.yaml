interface:
  display_name: "FDE Project Work"
  short_description: "Drive governed FDE work from scenario to evidence"
  default_prompt: "Use $fde-project-work to drive this operating problem through its next safe FDE delivery stage, produce the required artefacts, validate them, and report the gate result."
policy:
  allow_implicit_invocation: true
