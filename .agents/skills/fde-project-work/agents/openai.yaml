interface:
  display_name: "FDE Project Work"
  short_description: "Shape process-first, buildable FDE solutions"
  default_prompt: "Use $fde-project-work to analyse and improve one operating process, then select the simplest sufficient intervention—including AI only where justified—and define its first buildable vertical slice."
policy:
  allow_implicit_invocation: true
